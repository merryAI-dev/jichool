# 캘린더 자동 등록 설정

휴가를 `근태관리` 캘린더에 **자동으로** 올리기 위한 설정이다. 설정이 끝나면 이 스킬은 `.ics` 파일을 만들지 않고 일정을 직접 만든다.

## 이 저장소에 절대 넣지 않는 것

이 저장소는 공개되어 있고 전사에 배포된다.

- **서비스 계정 키 파일(JSON)** — 유출되면 도메인 전체 캘린더에 접근된다. 저장소, 이슈, 커밋 메시지, 스크린샷 어디에도 넣지 않는다
- 클라이언트 ID·시크릿, 토큰, 서버 URL의 인증 정보
- 특정 사용자의 이메일·별명·캘린더 ID

**설정값은 사람마다 다르다.** 개인 값은 각자 컴퓨터의 `~/.config/mysc-expense/state/leave-calendar.profile.json`(권한 600)에만 있고, 조직 값은 서버 환경변수·시크릿 관리 도구에만 있다. 둘 다 저장소로 오지 않는다.

## 왜 이 방식인가

각자 구글 로그인을 붙이는 방식은 **개인 컴퓨터마다 갱신 토큰이 남는다.** 그 토큰은 만료가 사실상 없어서, 그룹웨어 세션(로그아웃하면 무효)보다 다루기 어렵다.

서비스 계정 + 도메인 위임은 자격증명이 **서버 한 곳에만** 있다. 개인은 아무것도 보관하지 않고, 회수도 한 곳에서 끝난다. 대신 그 한 곳이 강력해지므로 **스코프를 캘린더로만 좁히는 것이 전제 조건**이다.

---

# 1부: 관리자가 한 번 (AXR팀 / Workspace 관리자)

개발 지식이 필요한 구간이다. 개인 사용자는 2부만 하면 된다.

**시작 전에 AXR팀과 보안·비용·정책을 논의한다.** 아래는 논의가 끝난 뒤의 절차다.

**① 서버 준비**
`taylorwilsdon/google_workspace_mcp`를 쓴다. 컨테이너로 배포하며 상태를 갖지 않는다.
조직 내부에서만 접근 가능한 곳에 둔다. 공개 인터넷에 열지 않는다.

**② Google Cloud 프로젝트와 서비스 계정**
프로젝트를 만들고 Google Calendar API를 사용 설정한다. 서비스 계정을 만들고 키를 발급한다.
**키 파일은 서버의 시크릿으로만 주입한다.** 파일로 주고받지 않는다.

**③ 도메인 전체 위임 — 스코프를 최소로**
Workspace 관리 콘솔에서 서비스 계정에 도메인 위임을 부여하되, **캘린더 스코프만** 넣는다.

```
https://www.googleapis.com/auth/calendar.events
```

Gmail·Drive·Sheets 스코프를 함께 넣지 않는다. 나중에 필요해지면 그때 별도로 검토한다.

**④ 도메인 허용목록**
서버 설정에서 사칭 대상을 회사 도메인으로 제한한다. 외부 주소로는 동작하지 않아야 한다.

**⑤ 대상 캘린더 권한**
`근태관리` 캘린더 공유 설정에서 서비스 계정에 **변경 권한**을 준다. 사용자를 사칭해 쓰더라도, 그 사용자에게 권한이 없으면 실패한다.

**⑥ 연결 확인**
관리자 본인 계정으로 일정 생성과 재조회가 되는지 확인한다. 테스트 일정은 지운다.

**⑦ 배포 안내**
구성원에게 서버 주소와 2부 절차를 공지한다. **키 파일이나 시크릿을 공지에 첨부하지 않는다.**

## 회수와 사고 대응

- 특정 구성원만 막을 때 — 캘린더 공유에서 그 사람 권한을 조정한다
- 전체를 즉시 끊을 때 — 서비스 계정 키를 폐기한다. 개인 컴퓨터에는 지울 것이 없다
- 키 유출이 의심되면 **즉시 폐기하고 재발급한다.** 노출된 키는 이미 위험하다

---

# 2부: 각자 한 번 (일반 사용자)

관리자 설정이 끝난 뒤, 한 번만 하면 된다. **여기서는 비밀번호도 키도 다루지 않는다.**

스킬이 이 절차를 안내할 때는 **한 번에 한 단계씩** 보내고 매번 확인받는다.

**① 설정 열기**
> 클로드 설정에서 **커넥터**(Connectors) 화면을 열어주세요.
> 화면이 열리면 알려주세요.

**② 서버 추가**
> AXR팀이 공지한 **주소**를 커넥터로 추가해주세요.
> 주소를 못 찾으시면 AXR팀에 문의하시면 돼요. 제가 알려드릴 수 있는 값이 아니에요.

**③ 회사 계정으로 로그인**
> 구글 로그인 창이 뜨면 **회사 계정**으로 로그인해주세요.
> 개인 계정으로 하시면 회사 캘린더가 안 보여요.

**④ 연결 확인**
> 목록에 `연결됨` 으로 보이나요? 그렇게 보이면 거의 다 됐어요.

**⑤ 새로 시작하기** — 빠뜨리면 연결해도 안 붙는다
> 마지막으로 **클로드를 새로 시작**해주세요. 그래야 방금 연결한 캘린더가 잡혀요.
> 다시 오셔서 "휴가 캘린더에 올려줘" 라고 말씀해주시면 이어서 할게요.

두 번 이상 같은 곳에서 막히면 AXR팀 문의로 넘긴다. 조직 정책으로 막혀 있을 수 있으므로 **사용자 잘못처럼 말하지 않는다.**

---

# 설정 후 스킬의 동작

연결이 되면 [CALENDAR_HARNESS.md](CALENDAR_HARNESS.md)의 **연결 도구 경로**가 그대로 돈다. 코드는 바뀌지 않는다.

```
표식 검색 → 없으면 생성 → 저장된 일정 재조회 → verify → 링크 안내
```

`handoff`와 `.ics`는 **연결이 아직 없거나 조직 정책으로 막힌 사람을 위한 대비책**으로 남는다. 연결이 되어 있으면 쓰지 않는다.

**연결됐다는 사용자의 말만으로 성공을 전제하지 않는다.** 실제로 쓸 수 있는 도구가 있는지 보고 판단하고, 만든 일정은 반드시 재조회해서 확인한 뒤에 완료라고 말한다.
