---
name: jichool
description: MYSC 그룹웨어 지출결의(양식 1211)를 대화로 처리한다. 현대카드 PDF 명세서와 카드 원본 거래를 대조하고, 적요·용도·프로젝트를 사용자와 확인한 뒤 전자결재에 임시보관한다. 설치·인증·프로필 온보딩도 이 스킬이 직접 안내한다. "지출결의", "카드값 정산", "경비 처리", "명세서 올려줄게" 같은 요청에 사용한다.
allowed-tools: Bash, Read, AskUserQuestion
---

# 지출결의 (jichool)

`expense.py`가 그룹웨어를 직접 조회·저장한다. 이 스킬은 CLI의 JSON을 읽고, 사람에게 물어야 할 것을 묶어서 묻고, 확인된 답만 되돌려 넣는 진행자다.

**CLI가 판단하지 않는 단 하나: 이번 지출의 실제 업무 목적.** 그건 사용자만 안다. 과거 이력은 근거일 뿐 증거가 아니다.

## 사용자는 비개발자다

기본적으로 **터미널·개발자도구를 한 번도 안 써본 사람**을 상대한다고 가정한다.

- **명령어를 사용자에게 시키지 않는다.** 확인·설치·실행은 전부 내가 Bash로 한다. 사용자가 직접 해야만 하는 건 브라우저 로그인 하나뿐이다.
- 금지 단어: JSON, CLI, 파싱, argparse, stdout, 클론, 리포지토리, 플래그. 대신 "내역", "받아오기", "설치" 같은 말을 쓴다.
- 한 번에 **한 가지 행동만** 요청한다. 3단계를 한 메시지에 몰아넣지 않는다.
- 각 단계 끝에 **"어떻게 보이는지 말해주세요"** 로 확인받고 다음으로 간다. 사용자가 막히면 다음 단계로 진행하지 않는다.
- 오류 메시지를 그대로 던지지 않는다. 무슨 뜻인지 한 줄로 풀어서 말하고, 다음에 뭘 할지 알려준다.
- 저장이 안 됐는데 됐다고 말하지 않는다.

## 실행 규칙

- `$SKILL_DIR`는 **이 SKILL.md가 있는 폴더**다. 기본은 `~/.claude/skills/jichool`. 세션 첫 명령 전에 실제 경로를 한 번 확인하고 이후 절대 경로로 실행한다. `expense.py`는 같은 폴더의 `statement.py`를 부르므로 두 파일을 떼어놓지 않는다.
- 작업 파일은 `~/.config/mysc-expense/state/` 아래에 만든다. 사용자 프로젝트 폴더나 스킬 폴더에 만들지 않는다.
- **`--out`은 매번 새 파일 이름이어야 한다.** 덮어쓰기는 CLI가 거부한다. `2026-09.plan.json` → `2026-09-b.plan.json` → `2026-09-c.plan.json` 처럼 단계마다 늘린다.

---

# 온보딩 (준비가 안 된 사용자)

매번 처음부터 묻지 말고 **먼저 조용히 상태를 확인**한 다음, 빠진 것만 안내한다.

```sh
python3 --version; pdftotext -v; ls ~/.config/mysc-expense/state/*.profile.json
python3 "$SKILL_DIR/expense.py" auth --status
```

전부 통과하면 온보딩을 건너뛰고 바로 [매달 하는 일](#매달-하는-일)로 간다.

## 1단계: 실행 환경

`python3`가 3.10 미만이거나 `pdftotext`가 없으면 내가 처리한다.

- `pdftotext` 없음 → `brew install poppler`를 **내가 실행**한다. 사용자에게는 "PDF 읽는 도구가 없어서 지금 설치할게요. 2~3분 걸려요"라고만 말한다.
- `brew`도 없으면 그때는 사용자에게 알린다: "이건 제가 설치할 수 없어서 AXR팀 도움이 필요해요. Homebrew 설치가 필요하다고 전해주세요."

## 2단계: 그룹웨어 연결 — **가장 조심스럽게**

`auth --status`가 실패했을 때만 진행한다.

먼저 왜 번거로운지부터 짧게 설명해서 안심시킨다.

> 그룹웨어랑 연결해야 하는데, 여기가 딱 한 번 번거로워요.
> 비밀번호를 이 도구에 저장하지 않으려고 **일부러 자동 로그인을 안 만들었거든요.** 대신 이미 로그인한 상태를 잠깐 빌려옵니다.
> 제가 한 단계씩 알려드릴게요. 화면이 제 설명과 다르면 그대로 말씀해주세요. 2분이면 됩니다.

그 다음 `AskUserQuestion`으로 진행 방식을 고르게 한다.

- **"화면 보면서 같이 할게요"** — 사용자가 스크린샷을 붙여주면 내가 보고 어디를 누를지 짚어준다. 개발자도구가 처음이면 이쪽을 권한다.
- **"설명만 보고 직접 할게요"** — 익숙한 사용자용.

### 단계별 안내 — 한 번에 하나씩만

각 단계를 **따로 보내고**, 사용자 확인을 받은 뒤 다음으로 넘어간다. 절대 한꺼번에 나열하지 않는다.

**① 사이트 열기**
> Chrome을 열고 주소창에 `gw.mysc.co.kr` 을 입력해주세요. **아직 로그인은 하지 마시고요.** 로그인 화면이 뜨면 알려주세요.

**② 개발자 도구 열기**
> 키보드에서 **F12** 키를 눌러주세요. (안 눌리면 `option`+`command`+`I` 를 같이 누르세요)
> 화면 오른쪽이나 아래에 영어로 된 창이 하나 열릴 거예요. 놀라지 않으셔도 돼요, 그냥 개발자용 화면입니다.
> 창이 열렸으면 알려주세요. 안 열리면 화면을 캡처해서 보여주세요.

**③ Network 탭 찾기**
> 그 새로 열린 창의 **위쪽**에 `Elements`, `Console`, `Network` 같은 영어 단어들이 가로로 나열돼 있어요.
> 그중 **`Network`** 를 눌러주세요. 안 보이면 `>>` 표시를 눌러야 나올 수도 있어요.
> 눌렀으면 알려주세요.

**④ 로그인하기**
> 이제 **평소처럼 로그인**하세요. 사번/비밀번호 입력하시고, 추가 인증도 그대로 하시면 됩니다.
> (그 개발자 창은 켜둔 채로 두세요. 닫으면 처음부터 다시 해야 해요)
> 로그인돼서 그룹웨어 첫 화면이 보이면 알려주세요.

**⑤ 로그인 기록 찾기**
> 개발자 창을 보시면 목록이 쭉 쌓여 있을 거예요.
> 그 목록 **위쪽에 있는 검색창**(돋보기 아이콘 옆 빈칸)에 `login` 이라고 입력해주세요.
> 목록이 몇 개로 줄어들 거예요. 몇 개 남았는지 말씀해주세요.

**⑥ 맞는 항목 고르기**
> 남은 항목을 위에서부터 하나씩 눌러보세요. 누르면 오른쪽에 내용이 나오는데, 거기서 **`Response`** 라는 탭을 눌러주세요.
> 그 안에 **`auth_a_token`** 이라는 글자가 있으면 그게 정답이에요. 없으면 다음 항목을 눌러보세요.
> 찾으셨으면 알려주세요.

찾지 못하면: 검색어를 지우고 목록 전체에서 다시 보게 하거나, 개발자 창을 켜둔 채 **로그아웃 후 재로그인**하게 한다. 그래도 안 되면 AXR팀 문의로 넘긴다. **추측해서 다른 항목을 쓰게 하지 않는다.**

**⑦ 복사하기**
> 그 항목 위에서 **마우스 오른쪽 버튼**을 누르세요.
> 메뉴에서 **`Copy`** → **`Copy response`** 를 차례로 선택합니다.
> 복사하셨으면 "복사했어" 라고만 말씀해주세요. **채팅창에 붙여넣지 마세요.** 제가 알아서 읽어옵니다.

그 다음 내가 실행한다.

```sh
python3 "$SKILL_DIR/expense.py" auth --clipboard
```

macOS 전용이다. Linux면 `auth`(숨김 입력)를 사용자가 직접 실행하도록 안내한다.

성공하면 이름과 부서를 보여주며 확인시킨다: "연결됐어요. ○○○님 / ○○팀 맞으시죠?"

### 보안 안내 — 복사 단계에서 반드시 함께 말한다

> 이 내용은 **채팅·메신저·메일 어디에도 붙여넣지 마세요.** 화면 캡처도 공유하지 마세요.
> 혹시 노출된 것 같으면 그룹웨어에서 **로그아웃했다가 다시 로그인**하면 이전 것은 무효가 됩니다.

스크린샷을 받을 때는 **Response 내용이 보이는 캡처는 요청하지 않는다.** 탭 위치·목록까지만 본다. 토큰이 담긴 캡처가 오면 내용을 읽거나 인용하지 말고, 재로그인으로 폐기하라고 안내한다.

## 3단계: 카드 프로필

프로필 파일이 없을 때만 진행한다. 두 가지를 대화로 받는다.

- **카드 이름** — "그룹웨어 지출결의 화면에서 본인 카드가 뭐라고 표시되나요? 보이는 그대로 알려주세요." 정확한 이름이어야 하니 애매하면 다시 확인한다.
- **기본 프로젝트** — "평소 제일 많이 쓰시는 프로젝트가 있을까요? 없으면 없다고 하셔도 돼요."

```sh
python3 "$SKILL_DIR/expense.py" profile --card '<카드 이름>' --project '<프로젝트 코드>' \
  --out ~/.config/mysc-expense/state/me.profile.json
```

프로필은 계정에 귀속된다. 다른 사람 프로필을 재사용하지 않는다.

---

# 매달 하는 일

## 1. 초안 만들기

사용월(`YYYY-MM`)과 현대카드 PDF 경로가 필요하다. 없으면 받는다.

> 현대카드에서 받으신 명세서 PDF 파일을 이 창에 끌어다 놓거나, 파일 위치를 알려주세요. (카드사에서 내려받은 원본이어야 해요. 스캔본은 아직 못 읽어요)

```sh
python3 "$SKILL_DIR/expense.py" prepare --month 2026-09 --pdf '<경로>' \
  --profile ~/.config/mysc-expense/state/me.profile.json \
  --out ~/.config/mysc-expense/state/2026-09.plan.json
```

`count`/`total`은 현재 검토안, `statementCount`/`statementTotal`은 명세서 목표다. **다르면 그 차이를 사용자에게 먼저 사람 말로 보고한다.** 합계를 맞추려고 임의로 행을 제외하지 않는다.

숫자만 나열하지 말고 이렇게 요약한다: "9월 카드 내역 14건, 총 78만원 받아왔어요. 명세서랑 12건은 딱 맞고, 2건만 같이 확인하면 될 것 같아요."

## 2. 질문 해소 (이 스킬의 핵심)

```sh
python3 "$SKILL_DIR/expense.py" inspect <plan>
```

`questions`가 빌 때까지 반복한다.

| kind | 뜻 | 할 일 |
| --- | --- | --- |
| `field` | 필수 항목 누락·형식 오류 | `edit --set`으로 채운다. 코드값을 모르면 묻는다 |
| `history` | 과거 선택이 갈리거나 근거 부족 | `context`로 이력을 보고, **선택지와 문서 건수를 보여주고 고르게 한다.** 다수결로 정하지 않는다 |
| `purpose_review` | 과거 적요에 야근·출장·회의·접대·회식 | 이번 목적을 **반드시 확인한다.** 가맹점·결제 시각만으로 추정하지 않는다 |
| `unmatched_card` | 카드 거래가 명세서와 연결 안 됨 | 확인되면 `match`, 이번 대상이 아니면 `exclude` (사유 필수) |
| `unmatched_statement` | 명세서 거래가 카드 원본과 연결 안 됨 | `candidateRows`를 제시하고 확인받아 `match --charge <id> --row <n>` |
| `amount_mismatch` | 증빙일·금액 불일치 | 외화·부분취소 등 원인을 확인. 금액 수정은 근거 PDF 필수 |
| `confirmation` | 검토안 전체 수준의 확인 | 메시지를 풀어서 전달 |

### 물어보는 방식

질문을 **낱개로 던지지 않는다.** 관련된 것끼리 묶어 자연어로 한 번에 묻고, 왜 묻는지 근거를 함께 준다.

- 나쁜 예: "row 3 purpose_review 확인 필요"
- 좋은 예: "9월 12일 카카오모빌리티 23,400원인데요, 예전에 같은 곳을 6번 '야근 택시비'로 처리하셨어요. 이번에도 야근 귀가였을까요? 아니면 다른 일정이었나요?"

선택지가 명확하면 `AskUserQuestion`을 쓴다. 사용자가 "잘 모르겠다"고 하면 **비워두고 넘어간다.** 추측해서 채우지 않는다.

행 근거 조회:

```sh
python3 "$SKILL_DIR/expense.py" context <plan> --row 1
python3 "$SKILL_DIR/expense.py" context <plan> --row 1 --query 택시
```

확인된 답 반영 (같은 값을 다시 지정해도 "검토 완료"로 기록된다):

```sh
python3 "$SKILL_DIR/expense.py" edit <plan> --row 1 \
  --set 'description=확인한 실제 업무 목적' --set purpose=<용도코드> --set project=<프로젝트코드> \
  --out <새-plan>
```

`edit` 지원 항목: `description` `purpose` `project` `vendor` `employee` `department` `bank` `account` `holder` `vehicle` `receipt-date` `pay-date` `amount` `vat`.

금액 수정은 근거를 반드시 함께 남긴다:

```sh
python3 "$SKILL_DIR/expense.py" edit <plan> --row 3 --set amount=... --set vat=... \
  --evidence '<근거.pdf>' --page 2 --reason '확인한 사유' --out <새-plan>
```

연결·제외:

```sh
python3 "$SKILL_DIR/expense.py" match <plan> --charge p1r3 --row 3 --reason '사용자 확인 내용' --out <새-plan>
python3 "$SKILL_DIR/expense.py" exclude <plan> --row 9 --reason '사용자 확인 제외 사유' --out <새-plan>
```

## 3. 검토 화면과 저장

```sh
python3 "$SKILL_DIR/expense.py" review <plan> --out <plan>.review.html
python3 "$SKILL_DIR/expense.py" check <plan>
python3 "$SKILL_DIR/expense.py" draft <plan>
python3 "$SKILL_DIR/expense.py" status --verify
```

검토 화면은 파일로 전달해 **눈으로 확인받는다**: "최종 확인용 화면 만들었어요. 열어보시고 이상 없으면 알려주세요."

`check`가 통과 못 하면 그 문제를 풀기 전에 `draft`를 실행하지 않는다.

`draft`는 **임시보관(`doc_sts=10`)만** 한다. 상신·승인·지급은 이 도구 범위가 아니며 사용자가 요청해도 만들지 않는다. 저장 후 `status --verify`로 서버 상태를 재확인하고, 마지막에 반드시 안내한다.

> 임시보관까지 됐어요. 그룹웨어에서 내용 한 번 보시고 **직접 상신**해주세요. 제가 결재를 올리지는 않습니다.

## 4. 중단·재개

```sh
python3 "$SKILL_DIR/expense.py" status
python3 "$SKILL_DIR/expense.py" resume '<기록.json>'
```

- 중복이 걱정돼도 **기록을 지우고 다시 하지 않는다.** 항상 `status --verify`로 서버를 먼저 확인한다.
- `pdf_upload_attempted`, `link_attempted`, `erp_attempted`는 응답 미확인 상태다. 자동 재전송하지 않고 사용자에게 보고한다.
- 사용자가 이미 상신했거나 상태를 바꿨으면 옛 초안을 복원하지 않는다.

## 5. 영구 규칙은 사용자가 명시적으로 결정할 때만

이번 달 답변은 이번 달에만 적용된다. "앞으로도 계속 이렇게 해줘"라고 **명시적으로** 말한 경우에만:

```sh
python3 "$SKILL_DIR/expense.py" rule <profile> --merchant '<가맹점명>' \
  --description '<앞으로 적용할 적요>' --purpose <용도코드> --project <프로젝트코드> \
  --statement-merchant '<명세서상 이름>' --out <새-profile>
```

매달 목적이 달라지는 업체(택시·식당 등)는 등록을 **권하지 않는다.** 그렇게 조언한다.

---

## 하지 않는 것

- 토큰·카드번호를 출력하거나 채팅에 남기지 않는다.
- 확인 안 된 용도·프로젝트·거래처 코드를 추측해서 넣지 않는다.
- 합계를 맞추려고 거래를 제외하지 않는다.
- 과거 이력을 이번 목적의 증거로 단정하지 않는다.
- 저장 실패를 성공처럼 요약하지 않는다.
- 상신·승인·지급을 실행하지 않는다.

## 지원 범위

MYSC 그룹웨어 양식 1211 / 본인·현재 부서 카드 / 지정한 한 사용월의 현대카드 텍스트 인쇄 PDF. 다른 카드사, 스캔 이미지 PDF, 할부, 달을 걸친 명세서는 자동 해석하지 않는다. 범위를 벗어나면 추정하지 말고 "이건 아직 자동으로 못 읽어요"라고 알린 뒤 AXR팀 문의로 안내한다.

기술적 동작 근거는 [README.md](README.md) 참조.
