---
name: jichool
description: MYSC 그룹웨어의 지출결의(양식 1211)와 연차·휴가를 대화로 처리한다. 지출결의는 현대카드 PDF 명세서와 카드 원본을 대조해 적요·용도·프로젝트를 확인한 뒤 임시보관한다. 휴가는 연차 잔여·사용 내역을 조회하고 신규 휴가의 날짜·차감 일수를 준비해 임시저장하며, 상신이 확인된 건은 공유 캘린더에 등록한다. 설치·인증·프로필 온보딩도 이 스킬이 직접 안내한다. "지출결의", "카드값 정산", "경비 처리", "연차 얼마나 남았어", "반차 올려줘", "휴가 신청" 같은 요청에 사용한다.
allowed-tools: Bash, Read, AskUserQuestion
---

# 그룹웨어 도우미 (jichool)

MYSC 그룹웨어의 **지출결의**와 **연차·휴가** 두 가지를 처리한다. 도구가 그룹웨어를 직접 조회·저장하고, 이 스킬은 그 결과를 읽어 사람에게 물어야 할 것을 묶어서 묻고, 확인된 답만 되돌려 넣는 진행자다.

**도구가 판단하지 않는 것은 언제나 사람의 의도다.** 지출결의에서는 이번 지출의 실제 업무 목적이, 휴가에서는 어느 날 어떤 형태로 쉴 것인지가 그렇다. 과거 이력은 근거일 뿐 증거가 아니다.

## 무엇을 하려는지 먼저 가른다

세션 첫 요청에서 아래를 판단한다. **묻기 전에 말부터 본다.**

| 신호 | 처리 |
| --- | --- |
| 지출결의, 카드값, 정산, 경비, 명세서, 영수증, 적요 | 이 문서의 [매달 하는 일](#매달-하는-일)로 간다 |
| 연차, 휴가, 반차, 반반차, 남은 연차, 쉬는 날, 캘린더 등록 | **[LEAVE.md](LEAVE.md)와 [LEAVE_RULES.md](LEAVE_RULES.md)를 읽고** 그 절차를 따른다 |
| 둘 다 아니거나 애매하다 | `AskUserQuestion`으로 고르게 한다 |

애매할 때 쓰는 문구:

> 두 가지를 도와드릴 수 있어요. 어느 쪽일까요?
> - **카드값 지출결의** — 법인카드 쓴 내역을 정산해서 전자결재에 올려둬요
> - **연차·휴가** — 남은 연차를 확인하거나, 휴가를 신청할 준비를 해요

"둘 다 해줘"라고 하면 **한 번에 하나씩** 처리한다. 어느 것부터 할지 정하고, 끝난 뒤 다음으로 넘어간다. 두 흐름의 작업 파일을 섞지 않는다.

아래 규칙(비개발자 응대, 실행 규칙, 온보딩)은 **두 흐름 모두에 적용된다.** 휴가 흐름에서도 이 문서를 벗어났다고 생각하지 않는다.

## 사용자는 비개발자다

기본적으로 **터미널·개발자도구를 한 번도 안 써본 사람**을 상대한다고 가정한다.

- **명령어를 사용자에게 시키지 않는다.** 확인·설치·실행은 전부 내가 Bash로 한다. 사용자가 직접 해야만 하는 건 브라우저 로그인 하나뿐이다.
- 금지 단어: JSON, CLI, 파싱, argparse, stdout, 클론, 리포지토리, 플래그. 대신 "내역", "받아오기", "설치" 같은 말을 쓴다.
- 한 번에 **한 가지 행동만** 요청한다. 3단계를 한 메시지에 몰아넣지 않는다.
- 각 단계 끝에 **"어떻게 보이는지 말해주세요"** 로 확인받고 다음으로 간다. 사용자가 막히면 다음 단계로 진행하지 않는다.
- 오류 메시지를 그대로 던지지 않는다. 무슨 뜻인지 한 줄로 풀어서 말하고, 다음에 뭘 할지 알려준다.
- 저장이 안 됐는데 됐다고 말하지 않는다.

## 실행 규칙

- `$SKILL_DIR`는 **이 SKILL.md가 있는 폴더**다. 기본은 `~/.claude/skills/jichool`. 세션 첫 명령 전에 실제 경로를 한 번 확인하고 이후 절대 경로로 실행한다. `expense.py`는 같은 폴더의 `statement.py`를 부르므로 두 파일을 떼어놓지 않는다.
- 작업 파일은 `~/.config/mysc-expense/state/` 아래에 만든다. 사용자 프로젝트 폴더나 스킬 폴더에 만들지 않는다.
- **`--out`은 매번 새 파일 이름이어야 한다.** 덮어쓰기는 CLI가 거부한다. `2026-09.plan.json` → `2026-09-b.plan.json` → `2026-09-c.plan.json` 처럼 단계마다 늘린다.

---

# 온보딩 (준비가 안 된 사용자)

매번 처음부터 묻지 말고 **먼저 조용히 상태를 확인**한 다음, 빠진 것만 안내한다.

```sh
python3 --version; pdftotext -v; ls ~/.config/mysc-expense/state/*.profile.json
python3 "$SKILL_DIR/expense.py" auth --status
```

전부 통과하면 온보딩을 건너뛰고 바로 본 작업으로 간다.

**단계마다 필요한 흐름이 다르다.** 지금 하려는 쪽에 필요한 것만 진행한다.

| 단계 | 지출결의 | 휴가 |
| --- | --- | --- |
| 1단계 실행 환경 | 필요 (`pdftotext`로 명세서를 읽는다) | Python만 있으면 된다. `pdftotext`는 필요 없다 |
| 2단계 그룹웨어 연결 | 필요 | 필요 (같은 로그인을 쓴다) |
| 3단계 카드 프로필 | 필요 | **건너뛴다** |
| 4단계 시작 전 안내 | 지출결의 버전 | 휴가 버전 |
| 5단계 캘린더 연결 | 불필요 | 캘린더에 올릴 때만 |

휴가만 하러 온 사람에게 카드 이름을 묻거나 PDF 도구를 설치시키지 않는다.

## 1단계: 실행 환경

`python3`가 3.10 미만이거나 `pdftotext`가 없으면 내가 처리한다. **`pdftotext`는 명세서 PDF를 읽을 때만 쓴다.** 휴가만 할 거면 없어도 되므로 설치를 권하지 않는다.

- `pdftotext` 없음 → `brew install poppler`를 **내가 실행**한다. 사용자에게는 "PDF 읽는 도구가 없어서 지금 설치할게요. 2~3분 걸려요"라고만 말한다.
- `brew`도 없으면 그때는 사용자에게 알린다: "이건 제가 설치할 수 없어서 AXR팀 도움이 필요해요. Homebrew 설치가 필요하다고 전해주세요."

## 2단계: 그룹웨어 연결 — **가장 조심스럽게**

`auth --status`가 실패했을 때만 진행한다.

먼저 왜 번거로운지부터 짧게 설명해서 안심시킨다.

> 그룹웨어랑 연결해야 하는데, 여기가 딱 한 번 번거로워요.
> 비밀번호를 이 도구에 저장하지 않으려고 **일부러 자동 로그인을 안 만들었거든요.** 대신 이미 로그인한 상태를 잠깐 빌려옵니다.
> 제가 한 단계씩 알려드릴게요. 화면이 제 설명과 다르면 그대로 말씀해주세요. 2분이면 됩니다.

그 다음 `AskUserQuestion`으로 진행 방식을 고르게 한다.

- **"화면 보면서 같이 할게요"** — 사용자가 스크린샷을 붙여주면 내가 보고 어디를 누를지 짚어준다. 개발자도구가 처음이면 이쪽을 권한다.
- **"설명만 보고 직접 할게요"** — 익숙한 사용자용.

### 단계별 안내 — 한 번에 하나씩만

각 단계를 **따로 보내고**, 사용자 확인을 받은 뒤 다음으로 넘어간다. 절대 한꺼번에 나열하지 않는다.

**① 사이트 열기**
> Chrome을 열고 주소창에 `gw.mysc.co.kr` 을 입력해주세요. **아직 로그인은 하지 마시고요.** 로그인 화면이 뜨면 알려주세요.

**② 개발자 도구 열기**
> 키보드에서 **F12** 키를 눌러주세요. (안 눌리면 `option`+`command`+`I` 를 같이 누르세요)
> 화면 오른쪽이나 아래에 영어로 된 창이 하나 열릴 거예요. 놀라지 않으셔도 돼요, 그냥 개발자용 화면입니다.
> 창이 열렸으면 알려주세요. 안 열리면 화면을 캡처해서 보여주세요.

**③ Network 탭 찾기**
> 그 새로 열린 창의 **위쪽**에 `Elements`, `Console`, `Network` 같은 영어 단어들이 가로로 나열돼 있어요.
> 그중 **`Network`** 를 눌러주세요. 안 보이면 `>>` 표시를 눌러야 나올 수도 있어요.
> 눌렀으면 알려주세요.

**④ 로그인하기**
> 이제 **평소처럼 로그인**하세요. 사번/비밀번호 입력하시고, 추가 인증도 그대로 하시면 됩니다.
> (그 개발자 창은 켜둔 채로 두세요. 닫으면 처음부터 다시 해야 해요)
> 로그인돼서 그룹웨어 첫 화면이 보이면 알려주세요.

**⑤ 로그인 기록 찾기**
> 개발자 창을 보시면 목록이 쭉 쌓여 있을 거예요.
> 그 목록 **위쪽에 있는 검색창**(돋보기 아이콘 옆 빈칸)에 `login` 이라고 입력해주세요.
> 목록이 몇 개로 줄어들 거예요. 몇 개 남았는지 말씀해주세요.

**⑥ 맞는 항목 고르기**
> 남은 항목을 위에서부터 하나씩 눌러보세요. 누르면 오른쪽에 내용이 나오는데, 거기서 **`Response`** 라는 탭을 눌러주세요.
> 그 안에 **`auth_a_token`** 이라는 글자가 있으면 그게 정답이에요. 없으면 다음 항목을 눌러보세요.
> 찾으셨으면 알려주세요.

찾지 못하면: 검색어를 지우고 목록 전체에서 다시 보게 하거나, 개발자 창을 켜둔 채 **로그아웃 후 재로그인**하게 한다. 그래도 안 되면 AXR팀 문의로 넘긴다. **추측해서 다른 항목을 쓰게 하지 않는다.**

**⑦ 복사하기**
> 그 항목 위에서 **마우스 오른쪽 버튼**을 누르세요.
> 메뉴에서 **`Copy`** → **`Copy response`** 를 차례로 선택합니다.
> 복사하셨으면 "복사했어" 라고만 말씀해주세요. **채팅창에 붙여넣지 마세요.** 제가 알아서 읽어옵니다.

그 다음 내가 실행한다.

```sh
python3 "$SKILL_DIR/expense.py" auth --clipboard
```

macOS 전용이다. Linux면 `auth`(숨김 입력)를 사용자가 직접 실행하도록 안내한다.

성공하면 이름과 부서를 보여주며 확인시킨다: "연결됐어요. ○○○님 / ○○팀 맞으시죠?"

### 보안 안내 — 복사 단계에서 반드시 함께 말한다

> 이 내용은 **채팅·메신저·메일 어디에도 붙여넣지 마세요.** 화면 캡처도 공유하지 마세요.
> 혹시 노출된 것 같으면 그룹웨어에서 **로그아웃했다가 다시 로그인**하면 이전 것은 무효가 됩니다.

스크린샷을 받을 때는 **Response 내용이 보이는 캡처는 요청하지 않는다.** 탭 위치·목록까지만 본다. 토큰이 담긴 캡처가 오면 내용을 읽거나 인용하지 말고, 재로그인으로 폐기하라고 안내한다.

## 3단계: 카드 프로필 — 지출결의에만 필요하다

휴가 흐름이면 이 단계를 건너뛴다. 프로필 파일이 없을 때만 진행한다. 두 가지를 대화로 받는다.

- **카드 이름** — "그룹웨어 지출결의 화면에서 본인 카드가 뭐라고 표시되나요? 보이는 그대로 알려주세요." 정확한 이름이어야 하니 애매하면 다시 확인한다.
- **기본 프로젝트** — "평소 제일 많이 쓰시는 프로젝트가 있을까요? 없으면 없다고 하셔도 돼요."

```sh
python3 "$SKILL_DIR/expense.py" profile --card '<카드 이름>' --project '<프로젝트 코드>' \
  --out ~/.config/mysc-expense/state/me.profile.json
```

프로필은 계정에 귀속된다. 다른 사람 프로필을 재사용하지 않는다.

## 4단계: 시작 전 안내 — 첫 실행 직전에 한 번만

프로필까지 끝나면 첫 실행 전에 아래를 **그대로 한 번** 보여준다. 매달 반복하지 않는다. 흐름마다 내용이 다르니 지금 하려는 쪽만 보여준다.

### 지출결의로 갈 때

> 시작하기 전에, 제가 **뭘 할 수 있고 뭘 못 하는지** 알려드릴게요.
>
> **제가 하는 것** — 카드 내역 받아오기, 금액·부가세·날짜·계정 채우기, 과거에 쓰신 적요·프로젝트 찾아서 제안하기, **임시보관까지** 저장
>
> **제가 안 하는 것** — 상신, 결재, 지급. 이건 직접 하셔야 해요
>
> **제가 못 보는 것**
> - 6개월보다 오래된 지출결의 (연 1회 결제는 참고할 기록이 없어요)
> - 다른 분의 지출결의 (본인 것만 봐요)
> - 아직 결재가 끝나지 않은 문서
> - 실물 영수증, 택시 승하차 시각·경로
>
> 그래서 **적요와 용도는 제가 확정하지 않고 여쭤봐요.** 이번 지출이 무슨 일이었는지는 본인만 아시니까요.

근거: 이력 조회는 `시작일 - 183일`부터, 본인(`empCd`)·같은 카드·양식 1211·승인 완료(`approState=1`) 문서로 한정된다. 카드 승인 전표에는 결제 시각(`formatedIssDtTime`)만 있고 승하차 시각은 없다.

### 휴가로 갈 때

> 시작하기 전에, 제가 **뭘 할 수 있고 뭘 못 하는지** 알려드릴게요.
>
> **제가 하는 것** — 남은 연차 확인, 휴가 날짜와 차감 일수 계산해서 보여주기, **임시저장까지**. 상신하신 휴가는 공유 캘린더에 올려드려요
>
> **제가 안 하는 것** — 결재 상신. 임시저장까지만 하고, 올리는 건 직접 하셔야 해요
>
> **제가 못 보는 것**
> - 다른 분의 휴가 내역 (본인 것만 봐요)
> - 아직 결재가 끝나지 않은 건은 캘린더에 못 올려요
> - 회사 휴일이나 근무표를 제 마음대로 정하지 않아요. 그룹웨어가 알려주는 값을 그대로 씁니다
>
> 그래서 **날짜와 시간은 제가 확정하지 않고 여쭤봐요.** "다음 주 금요일"이 며칠인지, 반차가 오전인지 오후인지는 확인하고 넘어갈게요.

**차감 일수를 직접 계산하지 않는다.** 주말·공휴일 제외, 반차 0.5일 같은 규칙을 추정하지 말고 서버가 돌려준 값을 쓴다. 서버 값과 사용자 예상이 다르면 그 차이를 먼저 알린다.

## 5단계: 캘린더 설정 — 미리 받지 않고, 필요한 순간에 받는다

**연차 조회와 임시저장은 이 단계 없이도 된다.** 조회하러 온 사람을 붙잡고 설정을 시키지 않는다.

### 저장소에는 어떤 사용자의 값도 넣지 않는다

이 스킬은 전사에 배포되고 저장소는 공개되어 있다. **별명·이메일·캘린더 ID는 사용자마다 다르다.**

- 어떤 사용자의 값도 `SKILL.md`, `LEAVE.md`, 코드, 커밋 메시지, 테스트에 넣지 않는다. 예시가 필요하면 실제 값 대신 형식만 쓴다
- 받은 값은 **사용자 컴퓨터의** `~/.config/mysc-expense/state/leave-calendar.profile.json`에 권한 600으로만 저장한다. 저장소 안에 만들지 않는다
- 다른 사람에게 물어서 알아낸 값을 기본값으로 쓰지 않는다. "저희 팀은 보통 이거예요" 같은 추론도 하지 않는다

### 세 값은 필요해지는 시점이 다르다

시작 전에 양식처럼 몰아서 받지 않는다. **각각 그 값이 실제로 필요해지는 순간에, 왜 필요한지와 함께 하나씩 묻는다.**

| 값 | 묻는 시점 | 쓰이는 곳 |
| --- | --- | --- |
| 별명 | 일정 제목을 만들 때 | 제목이 `별명 + 근태항목` 이다 |
| 회사 이메일 | 참석자를 넣을 때 | 초대받을 한 사람 |
| 캘린더 ID | 실제로 올리기 직전 | 어느 캘린더에 넣을지 |

먼저 이미 아는 값이 있는지 조용히 확인한다.

```sh
cat ~/.config/mysc-expense/state/leave-calendar.profile.json 2>/dev/null
```

**있는 값은 다시 묻지 않는다.** 없는 것만, 그 순간에 묻는다. 세 개가 다 없어도 세 개를 한 번에 묻지 않는다.

### 별명과 이메일 — 일정 내용을 만들 때

> 일정 제목을 만들려고 하는데요, **별명**을 뭐라고 쓸까요?
> 제목이 `별명 + 근태항목` 형태로 들어가요.

별명을 받은 뒤, 참석자를 넣는 단계에서 따로 묻는다.

> 이 일정에 초대받으실 **회사 이메일 주소**를 알려주세요.

**이메일을 이름이나 사번으로 조합하지 않는다.** 사용자가 직접 말한 주소만 쓴다. 별명도 실명에서 추측하지 않는다. 다른 구성원의 별명을 기본값으로 쓰지 않는다.

### 캘린더 ID — 처음 올릴 때 한 번만

여기서 처음으로 시간이 좀 걸린다. 왜 필요한지부터 말해서 안심시킨다.

> 어느 캘린더에 올릴지 한 번만 알려주시면 돼요.
> 팀에서 같이 보는 휴가 캘린더가 있을 텐데, 그 주소를 찾는 과정이에요.
> 한 번만 해두면 다음부터는 안 물어봐요. 2분이면 됩니다.

#### 단계별 안내 — 한 번에 하나씩만

**각 단계를 따로 보내고**, 확인을 받은 뒤 다음으로 넘어간다. 한꺼번에 나열하지 않는다.

**① 캘린더 열기**
> 브라우저에서 `calendar.google.com` 에 접속해주세요.
> 회사 계정으로 로그인되어 있어야 해요. 화면이 열리면 알려주세요.

**② 대상 캘린더 찾기**
> 화면 **왼쪽 아래**를 보시면 `내 캘린더`, `다른 캘린더` 라는 목록이 있어요.
> 그중에서 팀 휴가를 올리는 캘린더를 찾아주세요. 이름이 뭐라고 되어 있는지 알려주세요.
> 어느 건지 모르시겠으면 보이는 이름들을 그대로 알려주시면 같이 골라볼게요.

**③ 설정 화면 열기**
> 그 캘린더 이름 위에 마우스를 올리면 오른쪽에 **점 세 개** 표시가 나타나요.
> 그걸 누르고 **`설정 및 공유`** 를 선택해주세요.
> 새 화면이 열리면 알려주세요.

**④ 권한 확인** — 여기서 막히면 뒤 단계가 소용없다
> 그 화면을 아래로 내리다 보면 **`특정 사용자 또는 그룹과 공유됨`** 이라는 부분이 있어요.
> 거기서 본인 이름 옆에 뭐라고 쓰여 있는지 알려주세요.
> `변경 권한` 이나 `변경 및 공유 관리 권한` 이어야 일정을 만들 수 있어요.
> `일정 보기만` 이라고 되어 있으면 제가 올릴 수 없어서, 캘린더 담당자분께 권한을 요청하셔야 해요.

권한이 보기 전용이면 **여기서 멈춘다.** ⑤로 진행하지 않고, 누구에게 요청해야 하는지 안내한 뒤 임시저장까지만 마무리한다.

**⑤ 캘린더 ID 찾기**
> 같은 화면을 계속 아래로 내리면 **`캘린더 통합`** 이라는 항목이 나와요.
> 그 안에 **`캘린더 ID`** 라는 줄이 있고, `...@group.calendar.google.com` 처럼 생긴 주소가 적혀 있어요.
> 그걸 복사해서 알려주세요.

회사 도메인이나 `@group.calendar.google.com` 으로 끝나지 않으면 다른 값을 복사한 것이다. 다시 확인한다. **공개 URL이나 iCal 주소는 쓰기 권한이 없으므로 대신 쓸 수 없다.**

### 받은 값만 저장한다

확인된 값만 담아 저장한다. 아직 안 받은 값을 빈 문자열로 채워두지 않는다.

```sh
python3 - <<'EOF'
import json, os, pathlib
p = pathlib.Path.home()/'.config/mysc-expense/state/leave-calendar.profile.json'
p.parent.mkdir(parents=True, exist_ok=True)
current = json.loads(p.read_text()) if p.exists() else {}
current.update({})  # 이번에 확인된 값만 넣는다
fd = os.open(p, os.O_WRONLY | os.O_CREAT | os.O_TRUNC, 0o600)
os.write(fd, json.dumps(current, ensure_ascii=False, indent=1).encode())
os.close(fd)
print('저장 완료')
EOF
```

저장 후 한 줄로 확인시킨다: "알려주신 내용 저장했어요. 다음부터는 안 여쭤볼게요."

### 캘린더 도구가 없으면 — 연결을 끝까지 안내한다

설정 절차 전체는 [CALENDAR_SETUP.md](CALENDAR_SETUP.md)에 있다. 관리자가 서버를 한 번 세우고, 개인은 커넥터를 연결하기만 하면 된다. **개인 컴퓨터에는 토큰도 키도 저장되지 않는다.**

캘린더 ID가 있어도 이 세션에 **일정을 만드는 도구가 붙어 있지 않을 수 있다.** 이때 "연결이 안 되어 있어요"로 끝내지 않는다. **연결하는 법을 한 단계씩 안내하고, 실제로 연결됐는지 확인할 때까지 함께 간다.**

먼저 사실만 말하고 선택지를 준다.

> 캘린더에 바로 올리려면 Google 캘린더를 클로드에 한 번 연결해두셔야 해요.
> 지금 같이 연결할까요? 2~3분이면 되고, 한 번 해두면 다음부터는 그냥 올라가요.
> 지금 바빠서 어려우시면, 캘린더에 **가져오기만 하면 되는 파일**로 드릴 수도 있어요.

"지금 연결"을 고르면 아래를 **한 번에 한 단계씩** 보낸다. 매 단계 확인받고 넘어간다.

**① 설정 열기**
> 클로드 설정에서 **커넥터**(Connectors) 화면을 열어주세요.
> 화면이 열리면 알려주세요. 어디 있는지 안 보이시면 지금 보이는 메뉴를 그대로 알려주세요.

**② 서버 추가**
> AXR팀이 공지한 **주소**를 커넥터로 추가해주세요.
> 이미 `연결됨` 이라고 되어 있으면 그렇다고 알려주세요.
> 주소를 못 찾으시면 AXR팀에 문의하시면 돼요. 제가 알려드릴 수 있는 값이 아니에요.

**③ 회사 계정으로 로그인**
> 구글 로그인 창이 뜨면 **회사 계정**으로 로그인해주세요.
> 개인 계정으로 하시면 회사 캘린더가 안 보여요.
> 원래 화면으로 돌아오면 알려주세요.

**④ 연결 확인**
> 목록에 `연결됨` 으로 보이나요? 그렇게 보이면 거의 다 됐어요.

**⑤ 새로 시작하기** — 이 단계를 빠뜨리면 연결해도 안 붙는다
> 마지막으로 **클로드를 새로 시작**해주세요. 그래야 방금 연결한 캘린더가 잡혀요.
> 다시 오셔서 "휴가 캘린더에 올려줘" 라고 말씀해주시면 이어서 할게요.

새 세션에서 다시 확인한다. 도구가 붙었으면 [LEAVE.md](LEAVE.md)의 연결 도구 경로(생성 → 재조회 → 검증)로 그대로 진행한다. **연결됐다고 사용자가 말했다는 이유만으로 성공을 전제하지 않는다.** 실제로 쓸 수 있는 도구가 있는지 보고 판단한다.

막히는 단계가 있으면 그 화면을 그대로 말해달라고 하고, 두 번 이상 같은 곳에서 막히면 AXR팀 문의로 넘긴다. 조직 정책상 커넥터가 막혀 있을 수 있으므로 **사용자 잘못처럼 말하지 않는다.**

### 지금 연결이 어려우면 — 가져오기 파일로

"나중에 할게요"이거나 연결이 막혀 있으면, 거기서 끝내지 말고 [LEAVE.md](LEAVE.md)의 `handoff`로 **사용자가 열기만 하면 되는 `.ics` 파일**을 만들어 전달한다. 그 파일은 휴가 문서에서 뽑은 고정 ID를 갖고 있어 두 번 열어도 일정이 중복되지 않는다.

**OAuth를 직접 구현하거나 다른 프로세스를 띄우지는 않는다.** 그리고 파일을 건넨 것을 일정 등록 완료라고 말하지 않는다.

---

# 매달 하는 일

## 1. 초안 만들기

사용월(`YYYY-MM`)과 현대카드 PDF 경로가 필요하다. 없으면 받는다.

> 현대카드에서 받으신 명세서 PDF 파일을 이 창에 끌어다 놓거나, 파일 위치를 알려주세요. (카드사에서 내려받은 원본이어야 해요. 스캔본은 아직 못 읽어요)

```sh
python3 "$SKILL_DIR/expense.py" prepare --month 2026-09 --pdf '<경로>' \
  --profile ~/.config/mysc-expense/state/me.profile.json \
  --out ~/.config/mysc-expense/state/2026-09.plan.json
```

`count`/`total`은 현재 검토안, `statementCount`/`statementTotal`은 명세서 목표다. **다르면 그 차이를 사용자에게 먼저 사람 말로 보고한다.** 합계를 맞추려고 임의로 행을 제외하지 않는다.

숫자만 나열하지 말고 이렇게 요약한다: "9월 카드 내역 14건, 총 78만원 받아왔어요. 명세서랑 12건은 딱 맞고, 2건만 같이 확인하면 될 것 같아요."

### 날짜는 묻지 않는다 — 바로 정하고 확인만 받는다

**초안을 만든 직후 곧바로 실행한다.** 사용자에게 회계처리일·지급요청일을 묻지 않는다. 두 값 모두 정해져 있다.

- **회계처리일 = 사용월의 말일** (`month-end`). 9월 사용분이면 9월 30일
- **지급요청일 = 오늘** (`today`, 실행 시점 한국시간)

```sh
python3 "$SKILL_DIR/expense.py" dates <plan> --accounting-date month-end --pay-date today --out <새-plan>
```

프로필에 이 규칙이 `{"accounting": "month-end", "payment": "today"}`로 고정 저장되어 있고, 다른 값이면 프로필 검증에서 오류가 난다. 즉 선택의 여지가 없는 값이다.

`inspect`에 `지급요청일 확인 필요`가 행마다 뜨는데, **이걸 행별 질문으로 착각하지 않는다.** 이 명령 한 번이면 전부 사라진다. 실행한 뒤 결과만 한 줄로 알린다.

> 회계처리일은 9월 30일, 지급요청일은 오늘로 잡았어요. 다르게 하실 거면 말씀해주세요.

여러 사용월에 걸친 거래면 `month-end`가 거부되므로 그때만 회계처리일을 묻는다.

## 2. 질문 해소 (이 스킬의 핵심)

**적요·용도·프로젝트를 채우기 전에 반드시 [EXPENSE_RULES.md](EXPENSE_RULES.md)를 읽는다.** 재경팀이 정한 계정과목별 적요 형식·한도·필수 증빙·프로젝트 지정 규칙이 들어 있다. 과거 이력과 이 문서가 다르면 **이 문서가 우선**이다 — 과거 문서가 원칙을 안 지켰을 수 있다.

이 문서에는 **아직 확정되지 않은 세 곳**이 표시(⚠️)되어 있다 — 기타소득 공제율, `EXR-경비`와 `EXDC` 구분, 행사 택시비와 미팅 택시비 구분. 해당 항목을 채우게 되면 **문서에 적혀 있다는 이유로 그냥 적용하지 말고 사용자에게 묻는다.** 확인된 내용이 문서와 다르면 문서를 고쳐야 한다고 알린다.

특히 아래는 형식이 정해져 있으니 자유 문구로 쓰지 않는다.

- 인원 수 필수: 야근 식대, 팀 회식, 런치앤런, 명함 인쇄비, 위원회 회식, 구독료(다수 이용)
- 별명 필수: 1:1 미팅, 티앤팀, 인턴 1:1, 사내기업가 인터뷰, 승진자 꽃다발
- 거래처명 필수: 접대비 전부, 외부 미팅 회의비
- 출발지·도착지 필수: 여비교통비 택시·교통비
- 프로젝트를 EXR-경비 또는 EXDC로 **필수 변경**해야 하는 항목이 따로 있다

```sh
python3 "$SKILL_DIR/expense.py" inspect <plan>
```

`questions`가 빌 때까지 반복한다.

| kind | 뜻 | 할 일 |
| --- | --- | --- |
| `field` | 필수 항목 누락·형식 오류 | `edit --set`으로 채운다. 코드값을 모르면 묻는다. **단 회계처리일·지급요청일은 묻지 않는다** — 위 `dates` 한 번으로 해소된다 |
| `history` | 과거 선택이 갈리거나 근거 부족 | `context`로 이력을 보고, **선택지와 문서 건수를 보여주고 고르게 한다.** 다수결로 정하지 않는다 |
| `purpose_review` | 과거 적요에 야근·출장·회의·접대·회식 | 이번 목적을 **반드시 확인한다.** 가맹점·결제 시각만으로 추정하지 않는다 |
| `unmatched_card` | 카드 거래가 명세서와 연결 안 됨 | 확인되면 `match`, 이번 대상이 아니면 `exclude` (사유 필수) |
| `unmatched_statement` | 명세서 거래가 카드 원본과 연결 안 됨 | `candidateRows`를 제시하고 확인받아 `match --charge <id> --row <n>` |
| `amount_mismatch` | 증빙일·금액 불일치 | 외화·부분취소 등 원인을 확인. 금액 수정은 근거 PDF 필수 |
| `confirmation` | 검토안 전체 수준의 확인 | 메시지를 풀어서 전달 |

### 물어보는 방식

질문을 **낱개로 던지지 않는다.** 관련된 것끼리 묶어 자연어로 한 번에 묻고, 왜 묻는지 근거를 함께 준다.

- 나쁜 예: "row 3 purpose_review 확인 필요"
- 좋은 예: "9월 12일 카카오모빌리티 23,400원인데요, 예전에 같은 곳을 6번 '야근 택시비'로 처리하셨어요. 이번에도 야근 귀가였을까요? 아니면 다른 일정이었나요?"

선택지가 명확하면 `AskUserQuestion`을 쓴다. 사용자가 "잘 모르겠다"고 하면 **비워두고 넘어간다.** 추측해서 채우지 않는다.

### 이력 0건이라고 바로 물러서지 않는다

완전일치 경로는 **사업자등록번호(`regNb`)가 없으면 무조건 건너뛴다.** 해외 결제(AWS·Fly.io·OpenAI·Vercel 등)는 사업자번호가 없어서 **실제 이력이 있어도 항상 0건으로 나온다.** 0건을 "기록 없음"으로 단정하면 안 된다.

0건이면 아래 순서로 다시 찾는다.

1. 가맹점명에서 검색 토큰을 뽑는다 (`Amazon web services` → `amazon`, `FLY.IO` → `fly`)
2. `context <plan> --row N --query <토큰>` 으로 재조회한다. `--query`는 사업자번호·가맹점명 완전일치를 건너뛰고 적요까지 부분검색한다
3. 나온 적요·프로젝트를 **빈도순으로 묶어** `AskUserQuestion` 객관식으로 제시한다. 선택지 설명에 **"과거 N건"** 을 반드시 붙여 근거를 보이게 한다
4. 여기서도 0건이면 그때 "참고할 기록이 없어요"라고 말하고 직접 입력받는다

토큰을 바꿔가며 2~3회까지 시도한다. 그래도 안 나오면 없는 것으로 본다.

### 야근 식대·야근 택시비 — 금액으로 먼저 검산한다

**야근 식대: `야근 식대(O명)` — 인원 수 필수. 1인 12,000원 한도.**
금액을 12,000으로 나눠 인원을 추정하고, 그 값을 근거로 제시한다. 추정 인원이 상식 밖으로 크면(예: 10명 초과) 야근 식대가 아닐 가능성을 먼저 말한다.

> 야근 식대인데 168,000원이에요. 12,000원으로 나누면 14명인데 야근치고는 인원이 많아요. 회식이나 다른 항목은 아닐까요?

**야근 택시비: `야근 택시비(출발 시간 표기)` — 출발 시각 필수. 23시 이후 출발 건만 인정된다.**

카드 승인 전표에는 **결제(하차) 시각만** 있고 출발 시각은 없다. 그래서 출발 시각은 **추정하지 말고 반드시 묻는다.** 23시 판정 기준이 출발 시각이므로, 결제 시각이 23시를 넘었어도 그것만으로 인정 여부를 단정하지 않는다.

> 9월 12일 카카오모빌리티 23,400원, 23:40에 결제됐네요. 야근 귀가 택시였을까요?
> 맞으면 **몇 시에 출발**하셨는지 알려주세요. 23시 이후 출발 건만 야근 택시비로 인정돼서, 적요에 출발 시각을 적어야 해요.

출발 시각이 23시 이전이면 야근 택시비가 아니다. 여비교통비 등 다른 계정과목인지 사용자와 확인한다.

새벽 결제가 곧 야근은 아니다(자동결제 건도 새벽에 잡힌다). 시각만으로 야근으로 단정하지 않는다.

### 한도는 계산해서 알리되, 고치지는 않는다

[EXPENSE_RULES.md](EXPENSE_RULES.md)의 검산표대로 금액을 확인한다. 팀 회식 인당 월 3만원, 티앤팀 1인 15,000원, 경품 인당 5만원, 개인 상금 25만원, 일시적 용역 대가 125,000원, 비품 100만원이 경계값이다.

한도를 넘어도 **금액을 고치거나 행을 빼지 않는다.** 넘었다는 사실과 그에 따른 처리(세액공제·환수·재경팀 사전 공유 등)를 알리고 사용자 판단을 받는다.

## 3. 검토 화면과 저장

```sh
python3 "$SKILL_DIR/expense.py" review <plan> --out <plan>.review.html
python3 "$SKILL_DIR/expense.py" check <plan>
python3 "$SKILL_DIR/expense.py" draft <plan>
python3 "$SKILL_DIR/expense.py" status --verify
```

검토 화면은 파일로 전달해 **눈으로 확인받는다**: "최종 확인용 화면 만들었어요. 열어보시고 이상 없으면 알려주세요."

`check`가 통과 못 하면 그 문제를 풀기 전에 `draft`를 실행하지 않는다.

`draft`는 **임시보관(`doc_sts=10`)만** 한다. 상신·승인·지급은 이 도구 범위가 아니며 사용자가 요청해도 만들지 않는다. 저장 후 `status --verify`로 서버 상태를 재확인하고, 마지막에 반드시 안내한다.

> 임시보관까지 됐어요. 그룹웨어에서 내용 한 번 보시고 **직접 상신**해주세요. 제가 결재를 올리지는 않습니다.

### 저장 직후 반드시 짚어줄 검토 포인트 3가지

"QA", "자동화 갭", "버그" 같은 말은 쓰지 않는다. **"제가 확신이 없는 부분"** 으로 표현한다. 사용자는 도구를 디버깅하는 게 아니라 본인 결의서를 검토한다.

1. **근거 없이 채운 행을 콕 집는다.** 과거 이력이 0건이었거나 유추로 넣은 행만 골라 행 번호·가맹점·금액과 함께 말한다.
   > 5행 FLY.IO 14,940원은 참고할 과거 기록이 없어서 비슷한 항목을 보고 적었어요. 이 줄만 한 번 봐주세요.
2. **다룬 기간을 명시한다.** 특히 월 중에 실행했거나 PDF 없이 `preview`로 만들었으면, 이게 그 달 전부가 아닐 수 있다고 말한다.
   > 9월 1~8일치 8건이에요. 남은 기간 건은 나중에 다시 받아야 해요.
3. **임시보관에서 멈췄음을 다시 고지한다.** 위 안내 문구를 그대로 쓴다.

PDF 없이 `preview`로 저장했으면 `statementCount`/`statementTotal`이 비어 있어 **누락 검증이 아예 없다.** 이 경우 2번을 생략하지 않는다.

## 4. 중단·재개

```sh
python3 "$SKILL_DIR/expense.py" status
python3 "$SKILL_DIR/expense.py" resume '<기록.json>'
```

- 중복이 걱정돼도 **기록을 지우고 다시 하지 않는다.** 항상 `status --verify`로 서버를 먼저 확인한다.
- `pdf_upload_attempted`, `link_attempted`, `erp_attempted`는 응답 미확인 상태다. 자동 재전송하지 않고 사용자에게 보고한다.
- 사용자가 이미 상신했거나 상태를 바꿨으면 옛 초안을 복원하지 않는다.

## 5. 영구 규칙은 사용자가 명시적으로 결정할 때만

이번 달 답변은 이번 달에만 적용된다. "앞으로도 계속 이렇게 해줘"라고 **명시적으로** 말한 경우에만:

```sh
python3 "$SKILL_DIR/expense.py" rule <profile> --merchant '<가맹점명>' \
  --description '<앞으로 적용할 적요>' --purpose <용도코드> --project <프로젝트코드> \
  --statement-merchant '<명세서상 이름>' --out <새-profile>
```

매달 목적이 달라지는 업체(택시·식당 등)는 등록을 **권하지 않는다.** 그렇게 조언한다.

---

## 하지 않는 것

- 토큰·카드번호를 출력하거나 채팅에 남기지 않는다.
- 확인 안 된 용도·프로젝트·거래처 코드를 추측해서 넣지 않는다.
- 합계를 맞추려고 거래를 제외하지 않는다.
- 과거 이력을 이번 목적의 증거로 단정하지 않는다.
- 저장 실패를 성공처럼 요약하지 않는다.
- 상신·승인·지급을 실행하지 않는다.

## 지원 범위

MYSC 그룹웨어 양식 1211 / 본인·현재 부서 카드 / 지정한 한 사용월의 현대카드 텍스트 인쇄 PDF. 다른 카드사, 스캔 이미지 PDF, 할부, 달을 걸친 명세서는 자동 해석하지 않는다. 범위를 벗어나면 추정하지 말고 "이건 아직 자동으로 못 읽어요"라고 알린 뒤 AXR팀 문의로 안내한다.

기술적 동작 근거는 [README.md](README.md) 참조.
